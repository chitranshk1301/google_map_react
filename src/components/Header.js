// header with a logo only
import React from 'react';

const Header = () => (
    <header className='header'>
        <p className='logo'>
        Maps + React
        </p>
    </header>
);

export default Header;
